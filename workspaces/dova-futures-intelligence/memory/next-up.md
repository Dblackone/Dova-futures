# Next Up

1. Complete protected-governance registration of `dova-futures-intelligence` in `company/registry.md`.
2. Create a read-only `file_controller` compatibility adapter through the capability registry.
3. Add structured audit persistence for requests, policy decisions, tool calls, and results.
4. Add the first live provider adapter behind `ProviderRouter`, beginning with the locally supported runtime selected by the principal.
