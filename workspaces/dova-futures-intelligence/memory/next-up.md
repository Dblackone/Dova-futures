# Next Up

1. Complete independent security/checker review of TOOL-SUITE-01 before merge.
2. Install and start the official Voicebox Windows application only if local voice I/O is wanted, then verify the localhost MCP endpoint; keep voice samples and models outside git.
3. If authenticated World Monitor data is required, obtain a key and store it only in the local environment. Configure Agent Reach optional channels only with explicit approval and dedicated accounts.
4. Provide the destination GitHub repositories for the website and preorder store, then prepare a verified migration plan before touching deploy-critical paths.
5. Principal review of `drafts/DOVA_INTELLIGENCE_MASTER_BRIEF.md`; confirm first workflow, first indexed folder, provider/runtime, approval classes and backup boundary.
6. Promote the read-only `hub/` dashboard to the repository root only after the website migration is independently verified.
7. Replace remaining user-facing legacy labels in compatibility actions without changing stable tool identifiers.
8. Create a read-only `file_controller` compatibility adapter through the capability registry.
