# decisions.md — Decision Log (document-templates)

> Format: date — decision — alternatives considered — reason

- 2026-06-22 — Templates are pure HTML/CSS with contenteditable + window.print() — considered PDF-generation tooling and docx-only — because zero dependencies work in any browser, print-to-PDF is built in, and non-technical staff can use them unaided.
- 2026-06-22 — Both .html and .docx versions maintained per template — considered HTML-only — because some recipients/workflows require Word compatibility.
- 2026-08-03 — Deterministic document builder isolated at `documents/company-document-builder/`, with the approved quote design converted to Jinja2 and WeasyPrint retained as the primary renderer — considered editing the canonical static template or adding LLM generation first — because the engine must keep content, structure and design separate; Playwright/Chrome is documented as the Windows fallback when GTK/Pango is unavailable.
- 2026-08-06 — Keep the portable DOVA brand package as a version-controlled Codex skill under `.agents/skills/dova-company-brand/` and expose it globally through a directory junction — considered repo-local instructions only, a copied global skill, and a standalone design document — because one skill can carry policies, progressive references, reusable assets, and Codex discovery while the junction prevents a second independently edited copy; canonical `company/` files retain precedence over the packaged snapshot and legacy design-system narrative — logged by @lead/vector [codex].
- 2026-08-06 — Exclude the compiled design-system demo bundle from the portable skill and quarantine the supplied raster logo inventory — considered copying every source byte, silently editing stale claims, and treating conflicting artwork as approved — because reusable assets must not propagate unverified project metrics or the historical tagline; the active fallback lockup is extracted from the canonical letterhead fixed header and replacement primary artwork remains a principal decision — logged by @lead/vector [codex].
- 2026-08-11 — Website handover references the canonical design-system CSS/manifest and flags stale README claims — considered copying the design-system narrative as current website copy — because visual tokens/components are reusable but public claims must remain governed by `company/brand.md` and supplied evidence.
