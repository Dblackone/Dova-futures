# done-log.md — Completed Work (append-only, document-templates)

> Format: date — what — outcome — verified by
> (Entries before 2026-07-09 migrated from the old global memory/done-log.md.)

- 2026-06-22 — TEMPLATES-01 — 9 standalone print-ready HTML document templates + gallery index created from Claude Design export; contenteditable fields, window.print(), A4 @page CSS, DOVA brand tokens — verified by manual file review
- 2026-07-06 — Document template guidelines (context/05, now company/document-policy.md) + /dova-doc chat skill added (commit 534ac6e) — binding usage rule for all sessions — verified by review
- 2026-07-10 — TEMPLATES-02 — Letterhead added as template 00 (commit 11c4e77, pre-hub on main): `project/templates/00-Letterhead.html` + `.pdf` and `project/docx/00-Letterhead.docx`, generated by new `scripts/gen-letterhead-docx.mjs` (uses `docx` + `sharp` npm packages, added to root package.json) — verified by review (migrated note)
- 2026-07-31 — TEMPLATES-03 — `DOVA-Document-System.md` written: portable offline reference for the 10-template system (per-template field inventories extracted from the production HTML, numbering conventions, brand constants, draft→file workflow). Surfaced reference-prefix drift between `documents/README.md` and the HTML in 4 templates (02 `QUO-`/`QTE-`, 09 `MPA-`/`MPR-`, 06 + 07 use `DOVA/MEMO|LTR/YYYY/XXX` not `IL-`/`EL-`); logged as an open item, not fixed — docs only, no template edited — **pending checker verification**
- 2026-08-02 — TEMPLATES-04 — Document System **Phase 2**: ten sample documents, one per template, in `drafts/samples/` (`SAMPLE-00`…`SAMPLE-09` + `README.md`). One fictional client and project (Aterin Heights Limited, job `SAMPLE-2026-ATH-001`) run through quote → invoice → progress report → milestone claim → completion, with every figure reconciling exactly to a ₦97,779,000 ex-VAT contract sum. No template modified; nothing written to `projects/` or to `bim-standards/registers/project-register.csv`. Measured every sample and every blank template in headless Chrome — **five templates overflow A4 while empty despite hardcoding "Page 1 of 1"** — plus six further template defects, all logged to root `memory/triage.md`. Bebas Neue + Inter verified loading in all ten. No PDFs rendered (reason in the sample README §4). Maker output — **gated on the principal's review; Phase 3 does not start until then** — by @docs/quill [claude-code] — **pending checker verification**
- 2026-08-03 — OFFLINE-DOC-BUILDER — Added the uncommitted deterministic engine at `documents/company-document-builder/`: registry, JSON Schema validation, Decimal quotation calculations, Jinja2 HTML build, A4 CSS, WeasyPrint-first PDF render with documented Playwright/Chrome fallback, quotation example, tests and README. Verified by @qa/quartz [codex]: 11 pytest tests passed; HTML and A4 PDF generated. WeasyPrint itself remains blocked on this host by missing `libgobject-2.0-0` GTK/Pango runtime.
- 2026-08-03 — OFFLINE-DOC-BUILDER-FIX — Removed page-level A4 side margins, added the documented `data-wrap` print reset, anchored the branded footer within the print sheet, and regenerated the quotation HTML/PDF. Verified by print preview, A4 media-box inspection, and 12 pytest tests.
- 2026-08-03 — OFFLINE-DOC-BUILDER-PAGINATION — Converted the quotation footer to a repeated print footer and added dynamic page numbering through the PDF renderer. Verified PDF text extraction shows the identifiable footer on page 1 as `Page 1 of 2` and on page 2 as `Page 2 of 2`; no footer-only spill page remains.
- 2026-08-03 — OFFLINE-DOC-BUILDER-RUNNING-HEADER — Implemented the attached-report pagination pattern: page one retains full letterhead; page two onward uses a compact DOVA running header and terracotta rule; every page retains a live footer. Verified visually against the supplied seven-page report and the generated quotation PDF.
- 2026-08-06 — DOVA-BRAND-SKILL — Packaged `.agents/skills/dova-company-brand/` with canonical company policy snapshots, a semantic `DESIGN.md`, exact CSS/design-system assets, supplied logo variants, Codex UI metadata and source provenance; installed globally through `C:\Users\User\.codex\skills\dova-company-brand` for cross-repository sessions — by @lead/vector [codex] — skill validator passed, source hashes matched, and a fresh-agent landing-page concept passed the forward test; pending independent checker approval.
- 2026-08-06 — DOVA-BRAND-SKILL-QA — ❌ REJECT: copied logo lockups and `_ds_bundle.js` expose the retired “Rethink the future” line and unverified metrics/project claims despite the package’s prohibition; the forward-test concept directs use of the complete stale lockups; `openai.yaml` uses a 1×1 white PNG as `icon_small`; the branch has no candidate commit because `HEAD` equals `main` and the package remains untracked; fresh Codex implicit discovery could not be independently demonstrated on this host — verified by @qa/vera [codex].
- 2026-08-06 — DOVA-BRAND-SKILL-REV1 — Removed the compiled demo bundle and manifest containing unverified claims; moved all conflicting raster artwork and two 1×1 placeholders into prohibited `assets/legacy-logos/`; extracted a clean canonical-tagline SVG mark and lockup from the fixed letterhead header; pointed Codex UI metadata to the valid SVGs; made ethics hard limits non-overridable; updated provenance and usage rules; and passed a clean context-isolated forward test — by @lead/vector [codex] — pending checker re-review of the committed candidate.
- 2026-08-06 — DOVA-BRAND-SKILL-DISCOVERY — A separate context-isolated agent, given only `$dova-company-brand` and no filesystem path, discovered the globally installed skill and produced a ten-point homepage compliance checklist using the canonical tagline, safe letterhead lockup, semantic tokens, verified contacts, ethics limits and accessibility rules with no stale claims — by @lead/vector [codex] — evidence in gitignored `sandbox/dova-brand-global-discovery/`; pending checker re-review.
- 2026-08-06 — DOVA-BRAND-SKILL-QA-REV1 — ✅ APPROVE commit `0212965`: canonical snapshots and sanitised CSS match source hashes; ethics precedence, legacy-asset quarantine, active SVG lockup/icons, metadata, validator, clean commit/diff, global pathless discovery, and revised forward test all passed; primary raster artwork remains a non-blocking principal decision — verified by @qa/vera [codex].
- 2026-08-06 — DOVA-BRAND-SKILL-QA-IDENTITY-CORRECTION — The preceding approval is retained as useful independent-instance technical evidence but does **not** close the repository's formal checker gate: the spawned checker ran on Codex and signed the unregistered pairing `@qa/vera [codex]`, while `REGISTRY.md` binds `@qa/vera` to `claude-code` and `models/codex.md` requires Codex-built work to be checked by Claude or Gemma. Commit `0212965` remains in review — corrected by @lead/vector [codex].
- 2026-08-11 — TEMPLATES-05 — Created `documents/docx/DOVA-Futures-Offline-Template-Collection.docx`, an 18-page editable Word collection containing the blank letterhead and templates 01–09, each beginning on a fresh page. Recovered nine damaged source `.docx` packages in scratch, normalised the recovered copies through Microsoft Word, preserved the branded tables/placeholders/signature areas, and visually rendered all 18 pages for inspection — by @lead/vector [codex] — pending independent checker verification.
- 2026-08-11 — TEMPLATES-05-QA — ✅ APPROVE (technical independent Codex QA; not formal cross-model approval): Word opened the combined collection without repair, reported 18 pages / 2 sections / 50 tables, confirmed all ten starts in order, 161 editable placeholders, unprotected content, intact brand/legal details, empty unlinked continuation headers/footers, a valid 26-part OOXML package, and clean rendered contact sheets — verified by separate QA subagent [codex].
- 2026-08-11 — WEBSITE-HANDOVER-DS — Website handover includes the canonical design-system token inventory, component contract, source precedence and stale-claim warning; no design-system source files or tokens were changed — verified by direct inspection, by @lead/vector [codex]
