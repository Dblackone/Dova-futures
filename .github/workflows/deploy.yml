name: Deploy Hub to GitHub Pages

on:
  # Safety gate: dovafutures.com is still assigned to this repository's Pages
  # settings. Keep deployment manual until the marketing domain is transferred
  # to Dova-futures-website and hub.dovafutures.com is configured here.
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - uses: actions/checkout@v4

      - name: Setup Pages
        uses: actions/configure-pages@v5

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          # Publish only the read-only Hub. Never expose the private repository
          # tree or client/project records as part of the Pages artifact.
          path: hub

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
